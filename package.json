{
  "name": "custom-css",
  "description":
    "An extension for Google Chrome to add custom CSS on websites.",
  "scripts": {
    "format": "prettier --write 'src/**/*.js'",
    "pack":
      "rm -rf dist && mkdir dist && cp -r _locales manifest.json src dist/"
  },
  "license": "ISC",
  "dependencies": {
    "eslint": "^4.19.1",
    "prettier": "^1.12.1"
  }
}
